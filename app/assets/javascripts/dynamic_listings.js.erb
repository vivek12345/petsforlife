var listings= new Array();
    <% for listing in Listing.all %>
      listings.push(new Array('<%= listing.pet_type %>', '<%=listing.breed_type %>', '<%= listing.gender %>'));
    <% end%>

function filterSelected()
{
	var pet_type=$('.pet_type').val();
	var gender=$('.gender').val();
	var breed_type=$('.breed_type').val();	
	var filter_criteria=[];
	if(pet_type!="Pet")
		filter_criteria.push(pet_type);
	if(gender!="Gender")
		filter_criteria.push(gender);
	if(breed_type!="Breed")
		filter_criteria.push(breed_type);
	for(var i=0;i<filter_criteria.length;i++)
	{
		
	}

}
